#!/bin/bash
# safedb

# Stop on errors
set -Eeuo pipefail

# Command line options
usage() {
    echo "Usage $0 (create|destroy|reset|dump)"
}

if [ $# -ne 1 ]; then
    usage
    exit 1
fi

# Parse and run
case $1 in 
    "create")

    ;;
    "destroy")

    ;;
    "reset")

    ;;
    "dump")

    ;;
    *)
    usage
    exit 1
    ;;
esac